from distutils.core import setup

setup(
    name='gorgona',
    packages=[
        'gorgona',
    ],
    version='0.0.1',
    install_requires=[
        'numpy',
        'pandas',
        'matplotlib'
    ]
)
